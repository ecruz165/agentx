name: test-workflow
type: workflow
version: "1.0.0"
description: A test workflow with skill steps
runtime: node
steps:
  - id: analyze
    skill: skills/test/basic-skill
    inputs:
      path: .
  - id: analyze-again
    skill: skills/test/basic-skill
    inputs:
      path: ./src
