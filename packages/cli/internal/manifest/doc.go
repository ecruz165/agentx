// Package manifest handles parsing and validation of AgentX type manifests.
// It supports all six type manifests (context, persona, skill, workflow, prompt,
// template) and provides JSON Schema validation against the schemas defined in
// the packages/schema package.
package manifest