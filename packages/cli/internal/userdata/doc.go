// Package userdata manages the ~/.agentx/userdata/ directory structure including
// environment files, profiles, preferences, and per-skill registries. It handles
// initialization, path resolution, permission enforcement, health checks, and
// the env trace diagnostic for debugging environment variable resolution order.
package userdata